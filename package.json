{
  "name": "friday-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "friday": "bun run packages/cli/src/index.tsx",
    "dev": "bun --watch run packages/cli/src/index.tsx",
    "test": "bun test",
    "typecheck": "for p in shared providers tools mcp lsp core tui cli; do tsc --noEmit -p packages/$p/tsconfig.json || exit 1; done"
  },
  "dependencies": {
    "@opentui/core": "0.4.1",
    "@opentui/solid": "0.4.1",
    "solid-js": "1.9.13"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
