// @friday/core — headless engine (agent loop, sessions, modes, permissions, config).
// Implemented from M1 onward. Placeholder export keeps the workspace graph valid.
export const CORE_PLACEHOLDER = true
